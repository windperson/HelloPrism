<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="HelloPrism.Views.MainPage"
             Title="MainPage">
    <ScrollView Orientation="Vertical">
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Label Text="{Binding Title}"/>
            <Entry Placeholder="Please enter"/>
            <Editor Text="Multi Line\nAnother Line" HeightRequest="200" />
            <BoxView HorizontalOptions="Center" Color="Green" WidthRequest="50" HeightRequest="50"/>
            <Image Source="icon.png" WidthRequest="100" HeightRequest="100"/>
            <DatePicker Date="{Binding TheDate}"/>
            <TimePicker Format="HH:mm:ss" Time="{Binding TheTime}" />
            <WebView Source="https://www.google.com" HeightRequest="300" />
            <ActivityIndicator IsRunning="True" />
            <ProgressBar Progress="0.7"/>
        </StackLayout>
    </ScrollView>
</ContentPage>